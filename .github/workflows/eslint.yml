export default {
  env: {
    browser: false,
    es2021: true,
    node: true
  },
  extends: [
    "eslint:recommended"
  ],
  parserOptions: {
    ecmaVersion: "latest",
    sourceType: "module"
  },
  rules: {
    semi: ["error", "never"],
    quotes: ["error", "single"],
    indent: ["error", 0],
    "no-unused-vars": "warn",
    "no-undef": "error"
  }
}
